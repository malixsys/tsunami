/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {unoptimized: true},
    basePath: "",
    reactStrictMode: true,
    swcMinify: false,
    output: 'export',
};

export default nextConfig;
